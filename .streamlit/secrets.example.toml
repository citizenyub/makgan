# Streamlit secrets — copy this file to secrets.toml and fill in your key
# DO NOT commit secrets.toml (it's in .gitignore)
#
# Local dev:
#   cp .streamlit/secrets.example.toml .streamlit/secrets.toml
#   (then edit secrets.toml with your real key)
#
# Streamlit Cloud:
#   Add this in Advanced Settings → Secrets when deploying

ANTHROPIC_API_KEY = "sk-ant-api03-YOUR-KEY-HERE"
